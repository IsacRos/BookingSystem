version: '3.4'

services:
  bookingsystem.server:
    image: ${DOCKER_REGISTRY-}bookingsystemserver
    build:
      context: .
      dockerfile: BookingSystem.Server/Dockerfile
    ports:
    - "8080:8080"
    - "8081:8081"
    environment:
    - ASPNETCORE_ENVIRONMENT=Development
